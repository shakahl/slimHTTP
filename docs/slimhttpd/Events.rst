.. _slimhttpd-events:

slimHTTP events
===============

slimHTTP triggers a row of events depending on which state a user request is in. It also triggers events when the developer responds with data or data was statically found.
